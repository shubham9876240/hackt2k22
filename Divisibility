#include <iostream>
using namespace std;
int main()
{
    int N,sum=0;
    cin>>N;
    int data[N],arr[N];
    for(int i=0; i<N; i++)
        cin>>data[i];
    if(data[N-1]%10==0)
        cout<<"Yes";
    else
        cout<<"No";
 
    return 0;
}
